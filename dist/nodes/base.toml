[file]
name="base"
desc="Basic attributes that all nodes have"
inherits = [
    
]

# inputs and outputs are arrays of pairs of names and pin types
[inputs]

[outputs]

# Any attributes that exist in this file are known to exist for all nodes

[attrs]
name = ""
xpos = 0.0
ypos = 0.0
icon = ""
creation_date = 0